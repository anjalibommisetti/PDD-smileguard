import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  TextInput,
  ScrollView,
  Image,
} from "react-native";
import * as React from "react";
import { Snackbar } from "react-native-paper";

const Toaster = () => {
  return null; // For now, we'll use Paper's Snackbar directly in screens if needed
};

export { Toaster };
